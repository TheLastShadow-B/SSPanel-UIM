# 新增功能说明

> 本文档记录本轮新增/修复的功能。日期:2026-06-27。

---

## 一、修复:财务设置保存报错(已合入 `master`,已推送)

**现象**:后台 `/admin/setting/billing` 点保存报 `保存 cryptomus_currency 时出错`,导致**所有财务设置都存不了**(其中包括 Stripe 自动续费的几个开关)。

**根因**:`cryptomus_currency` 是 `class='billing'` 的配置项,但它的输入框在 `billing.tpl` 里被注释掉了。保存时后端循环写入每个 billing 项,这一项拿到 `null`;而生产库 `config.value` 是 `varchar(2048) NOT NULL`,写 `NULL` 触发 MySQL 1048 → `Config::set` 返回 false → 报错并中断整个保存。

**修复**(commit `9c52a6a8`,已推送 `origin/master`):
- `src/Models/Config.php`:`Config::set` 把 `null` 合并为 `''`(`$value ?? ''`),任何缺失字段都不再炸库。
- `resources/views/tabler/admin/setting/billing.tpl`:恢复 `cryptomus_currency` 输入框。
- `tests/TestDatabase.php`:把测试库 `value` 列对齐生产的 `NOT NULL`(此前是 nullable,掩盖了该 bug)。
- 新增/补充测试:`ConfigSetNullCoercionTest`、`BillingConfigTest`。

---

## 二、新增:订阅自动续费引擎(余额优先 + Stripe 卡兜底 + 宽限期)

> 状态:**后端引擎已完成并经多轮评审加固**,位于分支 `feat/stripe-balance-auto-renew`,**尚未合并到 `master`**。
> ⚠️ 当前为后端切片,**功能处于休眠**:还没有任何流程把订阅设为 `auto_renew=1`、没有绑卡入口、没有取消 UI。要真正对用户生效,需完成下文「未尽事项 Plan B/C」。

### 2.1 行为模型(续费决策树)

放弃 Stripe 原生订阅,改为**自管续费**(Stripe 仅作存档卡的一次性 off-session 扣款工具)。每日 cron 在到期当天对订阅执行:

```
到期当天(end_date <= today)& auto_renew=1 & 未取消:
├─ 余额(user.money)≥ 续费价(CNY renewal_price)  → 扣余额续期 ✓
└─ 余额不足:
   ├─ 有存档卡 → off-session 扣卡(CNY→stripe_currency 换算)
   │            ├─ 成功 → 续期 ✓
   │            └─ 明确被拒 → 进入宽限
   └─ 无存档卡 → 进入宽限

进入宽限(服务不中断,grace_until = end_date + stripe_grace_days(默认3)):
   ├─ 宽限内手动付清 → 续期、回 active
   └─ 超过宽限仍未付清 → 终止:降级(class=0)+ 作废续费账单(不可再支付)+ 邮件
```

要点:**先扣余额、不够刷卡、都不行进 3 天宽限(期间服务不断、可手动补款)、超期归零**;宽限内**不自动重试**;失败/终止发邮件。

### 2.2 核心代码

| 文件 | 内容 |
|---|---|
| `src/Services/SubscriptionService.php` | 引擎:`processAutoRenew()`(瀑布)、`payRenewalFromBalance()`(行锁扣余额)、`chargeRenewalToCard()`(claim→off-session 扣卡→结算)、`advanceRenewedPeriod()`(推进周期,**不再重置流量**,交给 `resetSubscriptionBandwidth`)、`enterGrace()`、`terminateLapsed()`;`expireSubscription()` 仅处理已取消(`auto_renew=0`)的自然过期 |
| `src/Services/Stripe/StripeService.php` | `chargeOffSession()`、`getDefaultPaymentMethod()`、`setCustomerDefaultPaymentMethod()` |
| `src/Services/Exchange.php` | 改为可注入(`getInstance`/`setInstance`),便于离线测试 CNY→外币换算 |
| `src/Controllers/User/InvoiceController.php` | `payBalance` 改为**行锁内复核状态再扣款**:防止与刷卡并发的双扣,且拒绝支付已作废(`cancelled`)账单 |
| `src/Command/Cron.php` | 每日任务顺序接入:`generateRenewalOrder → processAutoRenew → expireSubscription → terminateLapsed → …` |
| `src/Services/Cron.php` | `processPendingOrder` **豁免续费单**(`subscription_id` 非空)的 24 小时超时取消,使续费账单能存活到续费日 |
| `src/Services/Gateway/Stripe.php` | `notify`:对**没有 `trade_no`** 的 `payment_intent.succeeded`(自管续费的 off-session 扣款,cron 已同步入账)做安全 no-op,避免 500/重投 |
| `src/Models/Invoice.php` | 新增 `processing` 状态(扣款占位,防并发双扣);`status()` 增加文案 |
| `db/migrations/2026062701-set-grace-days-default.php` | 把 `stripe_grace_days` 默认值改为 `3`(仅当仍是默认 `7`/空时) |

### 2.3 配置项(后台财务设置)

- `stripe_auto_billing_enabled` —— 自动续费总开关(**注意:目前 `processAutoRenew` 尚未读取它做门控,需在 Plan B 接入,见未尽事项**)。
- `stripe_grace_days` —— 宽限天数(=3)。
- 复用:`stripe_api_key` / `stripe_publishable_key` / `stripe_endpoint_secret` / `stripe_currency` / `subscription_renewal_days`。

### 2.4 质量与评审

- 全程严格 TDD,`StripeService`/`Exchange` 全打桩、不触网;约 **78** 个相关单测通过。
- 经 **2 轮 opus 评审 + 2 轮 Codex 评审**,修复了一系列并发/支付状态机边界问题:
  - 卡扣前/结算行锁复核 `unpaid`,防双扣;`payBalance` 与刷卡并发的双扣竞态。
  - off-session 扣款**只在明确被拒时释放占位**;模糊错误(连接超时等)保留 `processing`,靠相同幂等键 `renew_inv_{id}` 对账重试,避免「已扣款但本地未入账」。
  - 续费单 24h 超时被取消导致**引擎根本不续费**的交互 bug。
  - 提前付款不再提前重置流量(原手动续费的现存回归)。
  - 宽限期内已归零的订阅不再每日重复扣款/重复发邮件。
  - `partially_paid`(部分支付)续费单不再卡死,进宽限后由 `terminateLapsed` 处理。

### 2.5 当前状态与未尽事项(重要)

**引擎是后端切片,目前对用户不可见、不生效**,因为:
- 正常下单流程**没有把订阅置 `auto_renew=1`**(只有待移除的 Stripe 原生订阅路径会置 1,且那条腿被排除在自管引擎外)。
- **没有绑卡入口**(余额不足时刷卡兜底需要用户先存卡)。
- **没有用户取消自动续费的 UI**。

要让功能真正可用,还需(尚未开始):

**Plan B(让引擎可达)**
- 改造首购:余额优先;不足走 Stripe Checkout 付首期并存卡(`setup_future_usage=off_session`),下单默认 `auto_renew=1`,去掉 `auto_renew_provider`。
- 改造 webhook:`payment_intent`/`checkout`/`setup_intent` 事件;移除原生订阅事件。
- 新增「支付方式」绑卡页(SetupIntent)。
- **给 `processAutoRenew` 接入 `stripe_auto_billing_enabled` 总开关门控**(评审已指出,上线前必须加)。

**Plan C(用户体验与收尾)**
- 「我的订阅」取消自动续费按钮 + 状态展示。
- 续费失败/失效邮件模板。
- 下单页文案。
- 删除原生 Stripe 订阅死代码(`PriceResolver::resolve`、`createSubscriptionCheckout`、`WebhookHandler` 原生分支等)。

### 2.6 关于 CNY/USD 换算(你的设置:Stripe=USD、产品按 CNY 定价)

- 续费金额以 CNY `renewal_price` 为权威,扣卡时经 `Exchange` 实时换算成 `stripe_currency`(USD)再按最小货币单位(零小数币种不 ×100)扣款。
- 注意:Stripe 有最低收款额(USD 约 $0.50),换算后过低的套餐会被拒;off-session 若卡需 3DS 会失败 → 进宽限(用户可在宽限内手动 on-session 完成验证)。

---

## 三、分支与合并

- 自动续费引擎在分支 `feat/stripe-balance-auto-renew`(未合并到 `master`)。
- 财务设置保存修复已在 `master` 并推送。
- 建议:待 Plan B/C 完成、功能端到端可用后再合并到 `master`(目前合并会引入若干**已对用户生效**的改动——`payBalance` 行锁、`processPendingOrder` 豁免续费单、续费不再提前重置流量等——这些已测试且为改进,但属上线行为变化)。
